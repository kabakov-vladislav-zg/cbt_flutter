import 'package:flutter/widgets.dart';

class CbtNotesFilter extends StatelessWidget {
  const CbtNotesFilter({super.key});
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 200,
    );
  }
}